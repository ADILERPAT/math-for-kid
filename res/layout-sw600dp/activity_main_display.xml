<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical" >

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal" >

        <ImageButton
            android:id="@+id/read_number_btn"
            android:layout_width="@dimen/width_image_main_display"
            android:layout_height="@dimen/width_image_main_display"
            android:contentDescription="@string/app_name"
            android:padding="10dp"
            android:src="@drawable/star" >
        </ImageButton>

        <ImageButton
            android:id="@+id/count_number_btn"
            android:layout_width="@dimen/width_image_main_display"
            android:layout_height="@dimen/width_image_main_display"
            android:contentDescription="@string/app_name"
            android:padding="10dp"
            android:src="@drawable/star" >
        </ImageButton>

        <ImageButton
            android:id="@+id/compare_max_btn"
            android:layout_width="@dimen/width_image_main_display"
            android:layout_height="@dimen/width_image_main_display"
            android:contentDescription="@string/app_name"
            android:padding="10dp"
            android:src="@drawable/star" >
        </ImageButton>

        <ImageButton
            android:id="@+id/compare_min_btn"
            android:layout_width="@dimen/width_image_main_display"
            android:layout_height="@dimen/width_image_main_display"
            android:contentDescription="@string/app_name"
            android:padding="10dp"
            android:src="@drawable/star" >
        </ImageButton>
    </LinearLayout>

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal" >

        <ImageButton
            android:id="@+id/plus_number_btn"
            android:layout_width="@dimen/width_image_main_display"
            android:layout_height="@dimen/width_image_main_display"
            android:contentDescription="@string/app_name"
            android:padding="10dp"
            android:src="@drawable/star" >
        </ImageButton>

        <ImageButton
            android:id="@+id/subtract_number_btn"
            android:layout_width="@dimen/width_image_main_display"
            android:layout_height="@dimen/width_image_main_display"
            android:contentDescription="@string/app_name"
            android:padding="10dp"
            android:src="@drawable/star" >
        </ImageButton>

        <ImageButton
            android:id="@+id/plus_and_subtract_btn"
            android:layout_width="@dimen/width_image_main_display"
            android:layout_height="@dimen/width_image_main_display"
            android:contentDescription="@string/app_name"
            android:padding="10dp"
            android:src="@drawable/star" >
        </ImageButton>

        <ImageButton
            android:id="@+id/remember_number_btn"
            android:layout_width="@dimen/width_image_main_display"
            android:layout_height="@dimen/width_image_main_display"
            android:contentDescription="@string/app_name"
            android:padding="10dp"
            android:src="@drawable/star" >
        </ImageButton>
    </LinearLayout>

</LinearLayout>